/**
 * Agent Index - Exports all specialized agents
 */

const BaseAgent = require('./BaseAgent');
const ResearchAgent = require('./ResearchAgent');
const CodingAgent = require('./CodingAgent');
const CreativeAgent = require('./CreativeAgent');
const AnalysisAgent = require('./AnalysisAgent');
const RecruiterAgent = require('./RecruiterAgent');
const SalesAgent = require('./SalesAgent');

module.exports = {
  BaseAgent,
  ResearchAgent,
  CodingAgent,
  CreativeAgent,
  AnalysisAgent,
  RecruiterAgent,
  SalesAgent,
};
